[project]
name = "summary-mcp-server"
version = "0.1.0"
description = "Paper Summaries MCP Server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.14.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"